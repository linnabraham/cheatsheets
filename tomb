# create a 100 MiB tomb called secret
tomb dig -s 100 secret.tomb
tomb forge secret.tomb.key
tomb lock secret.tomb -k secret.tomb.key

# open the tomb
tomb open secret.tomb -k secret.tomb.key

# by default the tomb is mounted in a directory inside /media (if not available it uses /run/media/$USER)
# If a second argument is given to the open command, it will indicate the  mount‐point  where the tomb should be made accessible. 
# The option -o can be used to pass mount options

# close the tomb
tomb close

# to change the password on a key file
tomb passwd -k path/to/key

# disregard warning against swap
-f

# use gpg key
tomb forge secret.tomb.key -g user@email.com -r sec-key-id -f
tomb lock secret.tomb -k secret.tomb.key -g user@email.com -r sec-key-id

# open the tomb using the gpg key this doesn't ask for password if the 
# gpg key is already unlocked with a passphrase
tomb open secret.tomb -g
