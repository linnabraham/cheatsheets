# smoothen curve
import scipy.signal.savgol_filter

# smoothen curve using gaussian kernel
from scipy.ndimage import gaussian_filter1d

# plot a beta(1,1) distribution
from scipy.stats import beta

x = np.linspace(0, 1, 100)
y = beta.pdf(x, 1, 1)
plt.plot(x,y)
