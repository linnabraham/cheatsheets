# fix for kitty hanging on exit
# https://github.com/kovidgoyal/kitty/issues/378
ctrl + shift + w

# change fonts
kitten choose-fonts

# needs a config to handle copy and paste

# install terminfo to remote server for tmux to work
kitty +kitten ssh suit

# run kitty with no config for debugging
kitty --config NONE
