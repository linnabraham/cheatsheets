# print to stdout
console.log(varname)

# install npm package globally
sudo npm -g install packagename

#Install it locally if you're going to require() it.
#Install it globally if you're going to run it on the command line.

# run js on the terminal
node file.js

# not operator
!

# check not null
if (user) {

console.log('hi')
}
