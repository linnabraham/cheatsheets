# aur package to install
claude-code

# in a new project directory just run
claude

# cycle between plan mode and others
shift-tab

# open an interactive picker to browse all recent sessions
claude --resume (or claude -r)
