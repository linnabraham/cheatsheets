# show playing music title
playerctl metadata title

# control tracks
playerctl next
playerctl previous

# show all players
playerctl --list-all # same as playerctl -l

# prefer a particular client
playerctl --player=vlc,%any play
